//#pragma once
//#include "source.h"

//using namespace std;

int main()
{
	/*
	vector<string> strs;
	
	string userdata = "";
	int n = 0;

	freopen_s(new FILE*, "input1.txt", "r", stdin);
	cin >> n;

	//ют╥б
	for (int i = 0; i < n; i++) {
		string temp;
		cin >> temp;
		strs.push_back(temp);
	}
	cin >> userdata;

	string ret = getScore(userdata, strs);
	cout << ret << endl;
	
	*/
	return 0;
}